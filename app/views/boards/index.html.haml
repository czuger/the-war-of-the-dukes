%h1 Listing boards

%table
  %thead
    %tr
      %th Owner
      %th Orf
      %th Wulf
      %th Turn

  %tbody
    - @boards.each do |board|
      %tr
        %td= board.owner.name
        %td= board.orf.name
        %td= board.wulf.name
        %td= board.turn
        %td= current_action_link( board )

%br

= link_to 'New Board', new_board_path