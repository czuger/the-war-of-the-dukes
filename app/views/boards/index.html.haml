%h1 Listing boards

%table
  %thead
    %tr
      %th Owner
      %th Opponent
      %th Turn
      -#%th
      -#%th
      -#%th
      %th
      %th

  %tbody
    - @boards.each do |board|
      %tr
        %td= board.owner.name
        %td= board.opponent.name
        %td= board.turn
        -#%td= link_to 'Orf setup', player_board_setup_path( @player, board, side: :orf )
        -#%td= link_to 'Wulf setup', player_board_setup_path( @player, board, side: :wulf )
        -#%td= link_to 'setup', board_setup_path( board )
        %td= link_to 'mouvement', board_movement_path( board )
        %td= link_to 'fight', board_fight_path( board )

%br

= link_to 'New Board', new_board_path